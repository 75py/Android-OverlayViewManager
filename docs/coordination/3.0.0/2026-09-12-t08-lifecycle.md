# T06統合とT08寿命管理

- 記録: Codex Astra / medium、2026-09-12 23:14 JST
- T08担当: Claude Sonnet5/high、独立レビューは別Sonnet5/high
- 状態: T08実装中、候補・実検証・承認は未完了

## 統合の根拠

PR49 head eb8e9f1a4367e8e29c4654e901fd4a1ff4c1c15aは、Claude承認5646384373、Codex対向承認5646396196、CI34698280430成功後、b105885ef61abe6ae42a6102994d8a348ef0335aへ通常merge（23:11:14 JST）。core101 tests成功、lintエラー0/警告13、assemble成功。T06の全dispatchはClaudeが解放済みと報告した。T05/T06の端末検証はT12のrelease-blocking項目として維持する。

PR50 head66c4686203d9835eb95346836bbd5ad309bfda2cは、Claude独立docsレビュー・承認5646401153、CI34698427542成功後、cd41b81f61656528772805e33a9d588e04f1e5acへ通常merge（23:13:14 JST）。共有work/3.0.0をfast-forwardした。

## T08の開始と分担

msg_827b2119bcb8で統合SHAと開始指示を渡し、msg_5250e4b8eb7dで実在する開始情報を受領した。

- Task: task_572d2116f4c5
- Dispatch: ctx_70332de7736c
- Terminal: term_073f016a-8ee6-4fae-ae50-7c2ff00f1e43
- Worktree: /Users/ai-seb/orca/workspaces/Android-OverlayViewManager/t08-lifecycle
- Branch: codex/t08-lifecycle
- Base: b105885ef61abe6ae42a6102994d8a348ef0335a
- Sonnet5/high/autoはClaude報告。チェックポイントはinit/factory、destructionと参照解放テスト、確定候補SHA。

合意済みF1〜F6は2026-09-12-t06-start.mdの協議・訂正・明示応答を参照。破棄時の即時remove1回と、成否にかかわらない参照解放・DISPOSED、既存classified failureの保持が中核。WeakHashMapだけで解放済みとはしない。Activity cache・handle・listener・解除callbackの参照を決定的に検証する。GC観測は補足に限定。APPLICATION opacity budget・失敗disposeのretry契約は変更しない。test reflectionを優先し公開JVM accessorを増やさない。Codexは指定Gradleの集約検証・対向承認・直列統合と計画/ログの単独編集を担当する。

## 23:19 JST T07寿命管理の次タスク合意

msg_3106de5da829の事前整理依頼に対し、msg_0aa52133f2f1でClaudeがL1〜L7を提案。既存DebugOverlayTreeはinitごとにcallback/overlayを作り直し、disposeがなく、nullable cacheを参照する。final OverlaySpecへの移行、show/hide結果確認、同一Application init冪等性、破棄済みActivityの全cache解除、明示disposeを次タスクとする。core/sample/lintの編集は禁止、T08統合後に開始する。

Codexはmsg_88691515f979でL2/L5を訂正。削除失敗でもhandleを捨ててuninitializedとする案ではattached windowの再試行手段を失うため、Java-callable MainThread dispose(): OverlayResultが先にcore disposeを試し、失敗時はhandle/callback/cache/buffer/初期化状態を保持して結果を返す。成功時だけcallback解除・所有参照解放・buffer消去・未初期化へ遷移し、再disposeはDISPOSED/changed=false成功。成功後のlog/renderはno-op、Timber.uprootはhost責務。

並行logが解放後に追加・null参照しないようactive境界をbufferLockで直列化し、queued renderはlifecycle generationを再確認する。dispose→reinit後に古い世代の内容を描画しないことを、キューと並行順序を制御したテストで確認する。同一Application再initは成功dispose後に許可、異なるApplicationはcoreの契約を維持する。private reflection/stub backendで失敗retryを検証し公開test APIを増やさない。msg_e7fd80e8ad86でClaudeは訂正版L1〜L7へ明示合意した。

## 23:21 JST T08実装チェックポイントの訂正

Codexが途中画面で新規containsActivityInstanceForTestsとpublic removeActivityInstance、破棄catch内の診断ログ欠落を確認しmsg_205e1a4335ceで合意事項の再徹底を依頼。msg_45a37a93d6cdでClaudeは全新規ForTests accessorの除去・private field reflection、production removeActivityInstanceのinternal化、classifyとログ出力の両方を担当へ拘束条件として伝達した。まだ候補SHAの完成確認ではない。

23:26 JST msg_e7a6296d99e5でcheckpoint(a)/(b)を受領。init/callbackの1回性・破棄済みfactory・複数handle・分類保持・構造的解放のテストを追加した一方、F3訂正は未反映だったためClaudeが候補確定前の反映を必須とした。23:28 JST msg_487895dc6b84で新規ForTests accessor全除去、private field reflection、removeActivityInstanceのinternal化、classifyとLogger.wの併用を確認したとの報告を受領。まだ候補SHA・実検証は未完了。

23:30 JST msg_4312eea2c7c6でCodexがtest側のcompanion backing fieldのreflection先誤り（outer classを使う）、remove1回/0回をtest名だけでなくrecording backendの回数でassertする不足、remove失敗時の構造的参照解放assert不足を指摘。msg_8f568119f176でClaudeは元担当へ候補確定前の必須修正として伝達した。新しい製品仕様ではなく、F2/F3の既存受け入れ条件の実証を補う。

23:31 JST msg_a6a265ebae3bで初回候補ccac98907e6dbee8a64aa68f0b17a468efbef6adを受領したが、3件のtest指摘は未反映。msg_ab3322fadabbで編集中checkoutへの古いSHA検証を省き、確定修正版だけを検証すると決定。opacity件数の16→17という報告も、実績17→18へ訂正依頼した。

23:32 JST msg_85d8a3c1d9d3でClaudeが「新修正dispatchを開始した」との前報を訂正。worker_done前の同じtask_572d2116f4c5 / ctx_70332de7736c内で修正中であり、新dispatch試行はactive dispatchのため拒否された。重複担当は起動していない。製品diffはccac989のsnapshotを独立レビューし、後続test差分だけ追加確認する。

## 23:36 JST T08実検証成功・PR51作成

msg_25c6d4074f3dの最終候補59ef303ac2aa94722d48c301ec9d88e104a91cc1（ccac989 + test-only修正）を受領し、HEAD・tracked cleanを確認。CodexがJDK17/所定SDKでcore全tests・lint・assembleを実行し成功した。XML111 tests、failure/error/skipped各0（ActivityLifecycleCleanup7、OverlayViewManager8、PassThroughOpacity18）、lintエラー0/警告13。

msg_f904d3bbeb81でClaudeはopacity件数17→18を正式訂正した。@Test funだけの検索では別行のannotationを数え落としていた。worker_doneはmsg_3810b445beb8、同dispatchの論理解放をClaude報告（外部管理terminalはidleのまま残る）。

候補をclaude/3.0.0/t08-lifecycleへpush、draft PR51を作成。CI34699740974起動中、独立レビュー/現SHA承認は未完了。msg_a0d7d5f62120で結果を共有。PR本文は最終挙動・実検証・T04c/T12未完了へ絞り、元本文のDISPOSED hideがfailureを返す誤記を除去（実装は従来どおりsuccess no-op）。ローカルGradleは完了し共有実行枠は空いている。
