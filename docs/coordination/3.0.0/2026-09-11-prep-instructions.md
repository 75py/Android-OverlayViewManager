# 準備: ユーザー指示の反映

- 日付: 2026-09-11（Asia/Tokyo、発言の正確な時刻は未記録）
- 状態: **ユーザー指示を反映。司令塔間の協議は未開始。**
- 記録担当: Codex
- 関連タスク: 準備、T00〜T13
- PR / Orca Run / Task / Dispatch: 未作成

## ユーザー指示（要旨）

1. 3.0.0用の作業ブランチと計画書を先に用意し、実装はまだ開始しない。
2. Codex（Astra / medium）とClaude（Fable 5.1 / high）が司令塔となり、それぞれサブエージェントを起動して分担する。
3. お互いのPRをレビューし、相手のAPPROVEを得てからマージする。
4. AGENTS.mdと必要なSerenaファイルは先にコミットしてよい。
5. PRタイトル・本文とソースコメントは英語、ユーザーとの会話は日本語とする。
6. サブエージェントはLuna、Terra、Sonnet 5を使い、司令塔がモデル・effortを判断する。GPT-5.5やHaikuは原則使用しない。
7. 計画書を適宜更新し、司令塔間のやり取りと判断結果もMarkdownでGit管理する。
8. 適宜Kotlinへ置き換える。

## 今回の反映内容と理由

- AGENTS.mdへ継続的に適用する指示を追加し、以前のサブエージェント選択方針より今回の指定を優先した。
- 計画書の「AGENTS.mdとSerenaは含めない」という旧方針を、ユーザーが許可した先行コミットへ更新した。
- `.serena/project.yml` はプロジェクト名・Java言語設定などの共有設定で、端末固有の絶対パスや認証情報を含まないことを確認した。既存 `.serena/.gitignore` と合わせて管理対象に選んだ。
- Serenaのcacheとproject.local.ymlは既存ignore規則で除外する。Kotlin言語サーバー設定は移行・ツール準備の段階で見直し、今回起動やインストールを行わない。
- Kotlin移行をT01の設計事項と各実装タスクの対象選定に追加した。Java consumerと公開APIへの影響を確認する方針にした。
- ログの保管場所・テンプレート・更新契機・単独提案と双方合意の区別を定義した。

## 今後の相談事項

- 実装開始後に両司令塔が具体的な担当・モデル設定・Kotlin移行対象を合意する。
- この文書はユーザー指示と準備内容の記録であり、Claudeとの相談や相互APPROVEが行われたことを示さない。
- 今回はローカル準備コミットまで。実装、エージェント起動、push、PR、マージは未開始。
